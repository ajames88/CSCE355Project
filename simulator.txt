# Below are the commands necessary to build and run my simulator submission.
# This portion of the project was completed in python v 3.7, so there are no build commands.
# The run command contains only the program file, additional input files must be added to the command in order to execute the program.

Build:
Run:
  python3.7 simulator.py
